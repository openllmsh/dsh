# dsh preview packages have unpublished transitive deps (dsh-environment) and
# are only needed at runtime (tsdown externalizes @deepseek-ai/*), so don't try
# to install the peer graph. The real services are provided by the running dsh.
auto-install-peers=false
strict-peer-dependencies=false
verify-deps-before-run=false
